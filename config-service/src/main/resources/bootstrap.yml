server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jeffersonecheverry20/microservice.git
          serchPath: config-data
          username: ${GIT_USER}jeffersonecheverry20
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
